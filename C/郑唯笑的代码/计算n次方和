#include<stdio.h>
int main()
{
	int i, j, n, k, sum = 0;
	scanf_s("%d", &n);
	for (i = 1; i <= n; i++)
	{
		k = i;
		for (j = 1; j < i; j++)
		{
			k = k * i;
		}
		sum = sum + k;
	}
	printf("sumΪ%d", sum);
	return 0;
}