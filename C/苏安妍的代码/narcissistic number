#include "stdio.h"
int main()
{
	int x=0,y=0,z=0;
	int i;
	
	for(i=100;i<=999;i++)
	{
		x=i%10;
		y=i/10%10;
		z=i/100%10;
		
		if(i==(x*x*x + y*y*y +z*z*z))
		{
			printf("%d is a narcissistic number\n",i);
		}
	}
    return 0;
}